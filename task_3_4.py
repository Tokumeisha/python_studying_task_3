line = input('Type something: ')
while line != ():
    line = list(line)
#l = length
    l = (len(line)//2)
    (len(line)%2)
    if len(line)%2 == 1:
       print(line[l])
    else:
        print('even number of characters')
    line = input('Type again: ')
